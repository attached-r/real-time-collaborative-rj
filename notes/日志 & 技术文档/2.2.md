 **日期**：2026年2月3日（周二）  
**今日完成模块**：用户模块基础（实体 + Repository + Service + 临时测试接口）

## 完成内容概述

- 定义了 User 实体类（MongoDB 映射）
- 创建 UserRepository（MongoRepository 继承）
- 实现 UserService（注册逻辑 + 密码加密 + 重复用户名检查）
- 添加临时测试接口 `/test-register`（GET，用于快速验证）
- 验证结果：在 MongoDB Atlas 的 `users` 集合中成功插入记录，密码已 BCrypt 加密，createdAt 自动填充

## 今日遇到的主要技术难点 & 解决方法

| 序号 | 难点描述                                                                 | 为什么是难点                                                                 | 解决方法                                                                                  | 关键代码 / 配置                                                                 |
|------|--------------------------------------------------------------------------|-----------------------------------------------------------------------------|-------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------|
| 1    | MongoDB 实体类映射 + 唯一索引 + 审计字段自动填充                         | 忘记加 `@Indexed(unique=true)` 会导致重复用户名插入；@CreatedDate 不生效 | 1. 加 `@Indexed(unique = true)` 于 username<br>2. 主类加 `@EnableMongoAuditing`          | `@Indexed(unique = true)`<br>`@EnableMongoAuditing` 在启动类上                  |
| 2    | 密码明文存储不安全，必须使用 BCrypt 加密                                 | 直接存明文密码是严重安全漏洞；Spring Security 默认不自动注入 PasswordEncoder | 在 SecurityConfig 中定义 `@Bean PasswordEncoder passwordEncoder()` 返回 BCryptPasswordEncoder | `@Bean public PasswordEncoder passwordEncoder() { return new BCryptPasswordEncoder(); }` |
| 3    | UserService 中注入 PasswordEncoder 失败（No qualifying bean）            | 依赖注入顺序问题或 SecurityConfig 未被扫描                                  | 确保 SecurityConfig 在组件扫描路径下，或显式 `@ComponentScan`；重启项目                  | `@Autowired private PasswordEncoder passwordEncoder;`                           |
| 4    | 开发期每次测试都要登录/注册，影响调试效率                               | 默认 Security 配置开启 formLogin/httpBasic，所有接口需认证                  | 临时修改 SecurityFilterChain：`.anyRequest().permitAll()` 或针对 `/test-*` permitAll     | `.requestMatchers("/test-*").permitAll()`<br>`.anyRequest().permitAll()`（临时） |
| 5    | 测试注册后去 Atlas 查看数据，密码加密字符串太长看不清                     | BCrypt 哈希很长，不直观                                                     | 无需解决（这是正常现象），重点验证前缀 `$2a$10$` 是否存在即可证明加密成功                | —                                                                               |
| 6    | createdAt / updatedAt 字段没有自动生成                                   | 忘记开启审计功能                                                            | 主启动类加 `@EnableMongoAuditing`                                                        | `@EnableMongoAuditing`                                                          |

## 今日关键代码片段

### User 实体（精简版）

```java
@Data
@Document(collection = "users")
public class User {
    @Id
    private String id;
    @Indexed(unique = true)
    private String username;
    private String password;  // BCrypt 加密后
    private String email;
    @CreatedDate
    private LocalDateTime createdAt;
}
```

### UserService.register 方法核心

```java
public User register(String username, String rawPassword, String email) {
    if (userRepository.findByUsername(username).isPresent()) {
        throw new RuntimeException("用户名已存在");
    }
    User user = User.builder()
            .username(username)
            .password(passwordEncoder.encode(rawPassword))
            .email(email)
            .build();
    return userRepository.save(user);
}
```

## 验证结果

- 接口：GET http://localhost:8080/test-register
- 响应示例：`注册成功！用户ID: 65f... , 用户名: testuser001 , 创建时间: 2026-02-03T...`
- MongoDB Atlas 截图确认：users 集合新增记录，密码已加密，createdAt 正常

## 心得 & 下一阶段准备

- 今天难点主要集中在 Spring Data MongoDB 的注解使用和 Security 依赖注入。
- 临时放开认证极大提高了开发效率（后续 JWT 完成后再收紧）。
- 明天计划：实现 JWT Token 生成、解析、过滤器 + 正式登录接口（返回 token）。

